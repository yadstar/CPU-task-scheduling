# CPU-task-scheduling
This project about CPU task scheduling that contains three algorithms in CPU scheduling, which contains: (First Come First Serve, Shortest Job First, Round Robin).

First, run the code then input two numbers for each process, then input a number for the quantum. And then the program should work and give you the results.
